# Refreshes vendon_events_cache (fast Delay Risk / Events reads for yesterday).
# Apply after: migrations/add_vendon_events_cache.sql on people_analytics.
# Schedule: once daily shortly after midnight UTC (adjust if you prefer Kuwait midnight).
apiVersion: batch/v1
kind: CronJob
metadata:
  name: vendon-events-cache-refresh
  namespace: leet-monitor
spec:
  schedule: "10 0 * * *"
  successfulJobsHistoryLimit: 2
  failedJobsHistoryLimit: 2
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      ttlSecondsAfterFinished: 900
      backoffLimit: 2
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: curl
              image: curlimages/curl:8.5.0
              imagePullPolicy: IfNotPresent
              env:
                - name: DASHBOARD_ACCESS_API_KEY
                  valueFrom:
                    secretKeyRef:
                      name: people-analytics-secrets
                      key: dashboard-access-api-key
              command:
                - /bin/sh
                - -c
                - >
                  exec curl -fsS
                  --connect-timeout 10
                  --max-time 900
                  --retry 2
                  --retry-delay 5
                  -X POST "http://people-analytics-api/api/vendon/internal/cache-events"
                  -H "Content-Type: application/json"
                  -H "X-Dashboard-Access-Secret:${DASHBOARD_ACCESS_API_KEY}"
                  -d "{}"

